version: "3.9"

services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_PASSWORD: jy9k60LsV1kWge9m
    volumes:
      - ./prisma/data:/var/lib/postgresql/data
    ports:
      - 5432:5432
